
import { createTheme } from "@mui/material";

export const cybTheme = createTheme({
  palette: {
    mode: "dark",
    primary: {
      main: "#E66C2C",
    },
    secondary: {
      main: "#ac0bce",
    },
    warning: {
      main: "#ffe358",
    },
    background: {
      // paper: "#181817",
    },
  },
});
